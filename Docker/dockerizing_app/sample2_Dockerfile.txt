# base image
FROM python:3.9

# workdir
WORKDIR /app

# copy all file to WORKDIR
#COPY <source> <destination>
COPY . /app

# run commands inside terminal
RUN apt-get update
RUN pip install -r requiremnts.txt
RUN apt-get upgrade -y

#ENTRYPOINT ["python","./app.py"]
CMD ["python","./app.py"]


//docker build -t <uname/imageName> .: to build an image.